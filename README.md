GoalPro — Personal Goal Manager (iPhone 11+)
============================================

This package contains the split files for the GoalPro demo app:

Files:
- index.html
- styles.css
- app.js
- README.md (this file)

How to run:
1. Unzip the package.
2. Open `index.html` in a modern browser (Safari on iPhone recommended).
3. The app uses localStorage; use Export to create a JSON backup.

Next steps (available on request):
- Add manifest.json + service-worker.js for PWA installability.
- Minify CSS/JS and prepare a GitHub Pages deployment.
- Improve drag-and-drop for touch (pointer/long-press fallback).
